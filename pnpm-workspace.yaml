packages:
  - "apps/*"
  - "packages/*"

# Builds nativos permitidos (deps con scripts de compilación).
# Las entradas de Prisma se añadirán cuando se cablee el driver Postgres.
allowBuilds:
  sharp: true
  unrs-resolver: true
  "@tailwindcss/oxide": true
  esbuild: true
